<%= form_for(@outside) do |f| %>
  <% if @outside.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@outside.errors.count, "error") %> prohibited this outside from being saved:</h2>

      <ul>
      <% @outside.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
